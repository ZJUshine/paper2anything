# paper2anything 统一凭据配置
# ──────────────────────────────────────────────────────────
# 用法：
#   1) 复制为 .env：   cp .env.example .env
#   2) 填入你自己的 key
#   3) 可选：把下面这行写进 shell 启动文件：
#         set -a; source <paper2anything 包根>/.env; set +a
#      （脚本内已 load_dotenv 兜底，即使不导出也能跑；已 export 的环境变量优先于 .env）
# 按需填写——不用的 skill 对应的 key 可留空。
# ──────────────────────────────────────────────────────────

# ── PDF 解析：MinerU 云端 API ──（在 https://mineru.net 申请；必填）
MINERU_API_TOKEN=your-mineru-token
# MINERU_API_BASE 是 API 端点根，skill 各自拼接 /api/v4；
# 一般无需改，除非走自建镜像/代理。务必填主机根，勿写成 .../api/v4。
MINERU_API_BASE=https://mineru.net

# ── OpenAI ──（paper2xhs / paper2wechat 的封面 AI 生成；可选）
# xhs/wechat 默认用它生成封面；留空则回退本地合成。
OPENAI_API_KEY=your-openai-or-proxy-key
OPENAI_BASE_URL=https://api.openai.com/v1
# 可选：覆盖封面生成的图像模型（默认 gpt-image-2.5-flare）。
# OPENAI_IMAGE_MODEL=gpt-image-2.5-flare

# ── paper2wechat md2wechat 排版/草稿箱 ──（可选）
# WECHAT_APPID / WECHAT_APP_SECRET：直推公众号草稿箱用。从微信开发者平台 developers.weixin.qq.com 获取 AppID/AppSecret，并在「API IP白名单」里加入本机出口 IP；草稿接口需实名认证。
# 留空则发布步骤降级为本地生成样式化 HTML 手动粘贴。
WECHAT_APPID=your-wechat-app-id
WECHAT_APP_SECRET=your-wechat-app-secret
MD2WECHAT_THEME=default

# ── paper2xhs 发布（可选）──
# 发布走开源项目 xiaohongshu-mcp（https://github.com/xpzouying/xiaohongshu-mcp）。
# 一般无需手动设——skill 执行到发布时会自动检查二进制、不存在则按平台下载到 ~/.paper2anything/xhs/。只在想用自定义二进制位置/版本时才设：
# XHS_MCP_BIN=/path/to/xiaohongshu-mcp-linux-amd64
# 自定义 mcp 服务地址/端口（默认 http://localhost:18060）：
# XHS_MCP_URL=http://localhost:18060
